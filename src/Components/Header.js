import React from 'react';

// Add functionality to load mainpage onCLick

const Header = () => {
    return (
        <div className="headerBox">
            <h1 className="headerText">Noteful</h1>
        </div>
    )
}

export default Header